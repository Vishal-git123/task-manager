# Cantilever Task Management - Frontend (Vite + React)

## Setup
1. In `frontend` folder run:
   ```
   npm install
   npm run dev
   ```
2. It expects backend API at `VITE_API_URL` (default `http://localhost:5000`).
3. Register -> Login -> Create tasks -> Filter & Sort.
